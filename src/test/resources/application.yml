spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gomo
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: 1111

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
      schema-locations: classpath:database/test-schema.sql
logging:
  level:
    org:
      springframework:
        test:
          context=DEBUG: