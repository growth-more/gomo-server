=== 설문 답변 등록

`POST /surveys`

사용자의 설문 답변 결과를 등록합니다.

==== 요청 헤더

operation::survey-answer-create[snippets='request-headers']

==== 요청 본문 (JSON)

operation::survey-answer-create[snippets='request-fields']

==== 성공 응답

성공 시 `201 Created` 상태 코드와 함께 빈 응답 본문을 반환합니다.

==== 에러 응답

operation::survey-answer-create-error[snippets='http-response,response-fields']

'''

=== 설문 목록 조회

`GET /surveys`

사용자 정보 수집을 위한 설문 문항 및 선택지 목록 전체를 조회합니다.

==== 요청 헤더

operation::survey-list-find[snippets='request-headers']

==== 성공 응답

operation::survey-list-find[snippets='http-response,response-fields']

==== 에러 응답

operation::survey-list-find-error[snippets='http-response,response-fields']
