=== 할당된 퀘스트 캘린더 조회

`GET /quests/assigns/calendars`

특정 날짜와 기간 단위를 기준으로 할당된 퀘스트 목록을 조회합니다.

==== 요청 헤더

operation::quest-assign-calendar-find[snippets='request-headers']

==== 쿼리 파라미터

operation::quest-assign-calendar-find[snippets='query-parameters']

==== 성공 응답

operation::quest-assign-calendar-find[snippets='http-response,response-fields']

// ==== 에러 응답

// operation::quest-assign-calendar-find-error[snippets='http-response,response-fields']

'''

=== 할당 퀘스트 완료

`PUT /quests/assigns/{id}/complete`

ID에 해당하는 할당 퀘스트를 '완료' 상태로 변경하고, 증명 내용을 기록합니다.

==== 요청 헤더

operation::quest-assign-complete[snippets='request-headers']

==== 경로 파라미터

operation::quest-assign-complete[snippets='path-parameters']

==== 요청 본문 (JSON)

operation::quest-assign-complete[snippets='http-request,request-fields']

==== 성공 응답

성공 시 `204 No Content` 상태 코드와 함께 빈 응답 본문을 반환합니다.

// ==== 에러 응답

// operation::quest-assign-complete-error[snippets='http-response,response-fields']

'''

=== 할당 퀘스트 확정

`PUT /quests/assigns/{id}/confirm`

ID에 해당하는 할당 퀘스트를 '확정' 상태로 변경합니다.
확정된 퀘스트는 수정하거나 삭제할 수 없습니다.

==== 요청 헤더

operation::quest-assign-confirm[snippets='request-headers']

==== 경로 파라미터

operation::quest-assign-confirm[snippets='path-parameters']

==== 성공 응답

성공 시 `204 No Content` 상태 코드와 함께 빈 응답 본문을 반환합니다.

// ==== 에러 응답

// operation::quest-assign-confirm-error[snippets='http-response,response-fields']

'''

=== 할당 퀘스트 생성

`POST /quests/assigns`

새로운 퀘스트를 생성하여 사용자에게 할당합니다.

==== 요청 헤더

operation::quest-assign-create[snippets='request-headers']

==== 요청 본문 (JSON)

operation::quest-assign-create[snippets='http-request,request-fields']

==== 성공 응답

operation::quest-assign-create[snippets='http-response,response-fields']

==== 에러 응답 (예: 퀘스트 내용 누락)

operation::quest-assign-create-error[snippets='http-response,response-fields']

'''

=== 반복 퀘스트 생성

`POST /quests/repeats`

매일/매주/매월 자동으로 생성될 반복 퀘스트를 등록합니다.

==== 요청 헤더

operation::quest-repeat-create[snippets='request-headers']

==== 요청 본문 (JSON)

operation::quest-repeat-create[snippets='http-request,request-fields']

==== 성공 응답

operation::quest-repeat-create[snippets='http-response,response-fields']

==== 에러 응답 (예: 퀘스트 제한 개수 초과)

operation::quest-repeat-create-error[snippets='http-response,response-fields']

'''

=== 할당 퀘스트 삭제

`DELETE /quests/assigns/{id}`

ID에 해당하는 할당 퀘스트를 삭제합니다.
이미 확정되거나 완료된 퀘스트는 삭제할 수 없습니다.

==== 요청 헤더

operation::quest-assign-delete[snippets='request-headers']

==== 경로 파라미터

operation::quest-assign-delete[snippets='path-parameters']

==== 성공 응답

성공 시 `204 No Content` 상태 코드와 함께 빈 응답 본문을 반환합니다.

// ==== 에러 응답

// operation::quest-assign-delete-error[snippets='http-response,response-fields']

'''

=== 반복 퀘스트 삭제

`DELETE /quests/repeats/{id}`

ID에 해당하는 반복 퀘스트를 삭제합니다.
더 이상 이 퀘스트는 자동으로 생성되지 않습니다.

==== 요청 헤더

operation::quest-repeat-delete[snippets='request-headers']

==== 경로 파라미터

operation::quest-repeat-delete[snippets='path-parameters']

==== 성공 응답

성공 시 `204 No Content` 상태 코드와 함께 빈 응답 본문을 반환합니다.

// ==== 에러 응답

// operation::quest-repeat-delete-error[snippets='http-response,response-fields']

'''

=== 진행 중인 퀘스트 목록 조회

`GET /quests/assigns`

현재 로그인된 사용자의 일간/주간/월간 퀘스트 목록 전체를 조회합니다.

==== 요청 헤더

operation::quest-assign-list[snippets='request-headers']

==== 성공 응답

operation::quest-assign-list[snippets='http-response,response-fields']

// ==== 에러 응답

// operation::quest-assign-list-error[snippets='http-response,response-fields']

'''

=== 반복 퀘스트 목록 조회

`GET /quests/repeats`

사용자가 설정한 반복 퀘스트 목록을 조회합니다.

==== 요청 헤더

operation::quest-repeat-list[snippets='request-headers']

==== 쿼리 파라미터

operation::quest-repeat-list[snippets='query-parameters']

==== 성공 응답

operation::quest-repeat-list[snippets='http-response,response-fields']

// ==== 에러 응답

// operation::quest-repeat-list-error[snippets='http-response,response-fields']

'''

=== 할당 퀘스트 정렬 순서 변경

`PUT /quests/assigns/orders`

특정 타입(일간/주간/월간)의 할당 퀘스트 목록의 표시 순서를 일괄 변경합니다.

==== 요청 헤더

operation::quest-assign-order-update[snippets='request-headers']

==== 요청 본문 (JSON)

operation::quest-assign-order-update[snippets='http-request,request-fields']

==== 성공 응답

성공 시 `204 No Content` 상태 코드와 함께 빈 응답 본문을 반환합니다.

==== 에러 코드

`DIS-ROO-001`: Order display must be positive +
`DIS-ROO-002`: Order display increment must be positive

// ==== 에러 응답
//
// operation::quest-assign-order-update-error[snippets='http-response,response-fields']

'''

=== 반복 퀘스트 정렬 순서 변경

`PUT /quests/repeats/orders`

특정 타입(일간/주간/월간)의 반복 퀘스트 목록의 표시 순서를 일괄 변경합니다.

==== 요청 헤더

operation::quest-repeat-order-update[snippets='request-headers']

==== 요청 본문 (JSON)

operation::quest-repeat-order-update[snippets='http-request,request-fields']

==== 성공 응답

성공 시 `204 No Content` 상태 코드와 함께 빈 응답 본문을 반환합니다.

==== 에러 코드

`DIS-ROO-001`: Order display must be positive +
`DIS-ROO-002`: Order display increment must be positive

// ==== 에러 응답
//
// operation::quest-repeat-order-update-error[snippets='http-response,response-fields']

'''

=== 할당 퀘스트 수정

`PUT /quests/assigns/{id}`

ID에 해당하는 할당 퀘스트의 내용을 수정합니다.
확정되지 않은 퀘스트만 수정 가능합니다.

==== 요청 헤더

operation::quest-assign-update[snippets='request-headers']

==== 경로 파라미터

operation::quest-assign-update[snippets='path-parameters']

==== 요청 본문 (JSON)

operation::quest-assign-update[snippets='http-request,request-fields']

==== 성공 응답

성공 시 `204 No Content` 상태 코드와 함께 빈 응답 본문을 반환합니다.

==== 에러 응답 (예: 퀘스트 내용 누락)

operation::quest-assign-update-error[snippets='http-response,response-fields']

'''

=== 반복 퀘스트 수정

`PUT /quests/repeats/{id}`

ID에 해당하는 반복 퀘스트의 내용을 수정합니다.

==== 요청 헤더

operation::quest-repeat-update[snippets='request-headers']

==== 경로 파라미터

operation::quest-repeat-update[snippets='path-parameters']

==== 요청 본문 (JSON)

operation::quest-repeat-update[snippets='http-request,request-fields']

==== 성공 응답

성공 시 `204 No Content` 상태 코드와 함께 빈 응답 본문을 반환합니다.

==== 에러 응답 (예: 퀘스트 내용 누락)

operation::quest-repeat-update-error[snippets='http-response,response-fields']
